namespace Domain.Enums;

/// <summary>
/// Represents the status of a request (friendship or shared plan).
/// </summary>
public enum RequestStatus
{
    Pending = 0,
    Accepted = 1,
    Rejected = 2
}
