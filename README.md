### Start środowiska dev

```bash
git clone https://github.com/Gringee/FitMateBackend
cd FitMateBackend
docker-compose up -d --build //Wymaga odpalonego Docker Desktop 

API	http://localhost:5000/swagger/index.html
DB	localhost:5433	admin / Admin123!
